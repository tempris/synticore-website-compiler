'use strict';

/**
 * @fileoverview cure-ansi — rich terminal styling, cleanup, and diagnostics for Node.js.
 *
 * Provides:
 * - Canonical style/color maps (fg, bg, bright).
 * - Cursor and screen helpers (movement, clears, hide/show).
 * - 256-color and true-color (RGB) helpers.
 * - Safe `format()` that guarantees reset and supports `[[ANSI_ON]]` / `[[ANSI_OFF]]`.
 * - `clean()` to preserve leading indentation with active style state.
 * - Visual test helpers to manually verify terminal capabilities.
 *
 * Environment:
 * - Honors {@link https://no-color.org/ NO_COLOR} by eliding all SGR sequences when set.
 *
 * @module cure-ansi
 * @version 1.0.0
 * @since 1.0.0
 * @license MIT
 * @author Cure Interactive
 *
 * @example <caption>Quick start (installed as a package)</caption>
 * const { ANSI } = require('cure-ansi');
 * const ansi = new ANSI();
 * console.log(ansi.format(ansi.fg.green + ansi.style.bold, 'Hello world'));
 *
 * @example <caption>Local usage (within this repo)</caption>
 * const { ANSI } = require('./index');
 * const ansi = new ANSI();
 * for (let i = 16; i < 24; i++) {
 *   process.stdout.write(ansi.getContrastFg(i) + ansi.color256.bg(i) + ` ${i} `);
 * }
 * process.stdout.write(ansi.reset + '\n');
 *
 * @see https://no-color.org/ — `NO_COLOR` community standard
 * @see https://en.wikipedia.org/wiki/ANSI_escape_code — ANSI SGR reference
 * @see https://jonasjacek.github.io/colors/ — xterm-256 color table
 */

/**
 * Shape of the active SGR state used by {@link ANSI#clean}.
 * @typedef {Object} ANSIActiveCodes
 * @property {string} foreground - Last applied **foreground** SGR (e.g., "\x1b[31m") or empty string.
 * @property {string} background - Last applied **background** SGR (e.g., "\x1b[44m") or empty string.
 * @property {string[]} formatting - Array of active **formatting** SGR sequences (bold, underline, etc.).
 */

/** @callback CursorNFn
 * @param {number} [n=1]
 * @returns {string}
 */

/** @callback CursorMoveToFn
 * @param {number} row
 * @param {number} col
 * @returns {string}
 */

/** @callback TestFn
 * @returns {void}
 */

/**
 * Map of simple SGR codes by name.
 * @typedef {Object.<string, string>} ANSISimpleMap
 */

/**
 * Foreground palette including bright variants.
 * @typedef {Object} ANSIForegroundPalette
 * @property {string} reset
 * @property {string} black
 * @property {string} red
 * @property {string} green
 * @property {string} yellow
 * @property {string} blue
 * @property {string} magenta
 * @property {string} cyan
 * @property {string} white
 * @property {ANSISimpleMap} bright
 */

/**
 * Background palette including bright variants.
 * @typedef {Object} ANSIBackgroundPalette
 * @property {string} reset
 * @property {string} black
 * @property {string} red
 * @property {string} green
 * @property {string} yellow
 * @property {string} blue
 * @property {string} magenta
 * @property {string} cyan
 * @property {string} white
 * @property {ANSISimpleMap} bright
 */

/**
 * Cursor control helper shape. Implemented as {@link ANSI#cursor}.
 * @typedef {Object} ANSICursor
 * @property {CursorNFn} up - Move cursor up `n` rows (default `1`).
 * @property {CursorNFn} down - Move cursor down `n` rows (default `1`).
 * @property {CursorNFn} right - Move cursor right `n` columns (default `1`).
 * @property {CursorNFn} left - Move cursor left `n` columns (default `1`).
 * @property {CursorMoveToFn} moveTo - Move cursor to 1-based `(row;col)`.
 * @property {string} show - SGR to show the cursor.
 * @property {string} hide - SGR to hide the cursor.
 * @property {string} save - Save current cursor position (VT100 compatible).
 * @property {string} restore - Restore previously saved cursor position.
 */

/**
 * Screen control helper shape. Implemented as {@link ANSI#screen}.
 * @typedef {Object} ANSIScreen
 * @property {string} clear - Clear entire screen.
 * @property {string} clearFromCursorToEnd - Clear from cursor to end of screen.
 * @property {string} clearFromCursorToStart - Clear from cursor to start of screen.
 * @property {string} clearLine - Clear entire line.
 * @property {string} clearLineToEnd - Clear from cursor to end of line.
 * @property {string} clearLineToStart - Clear from cursor to start of line.
 */

/**
 * High-contrast / balanced-contrast palettes.
 * @typedef {Object} ANSIPalette
 * @property {ANSISimpleMap} fg
 * @property {ANSISimpleMap} bg
 */

/**
 * Test helpers namespace methods available via {@link ANSI#test}.
 * @typedef {Object} ANSITestNamespace
 * @property {TestFn} standard - Visual test of basic SGR styles and 8/8 bright sets.
 * @property {TestFn} color256 - Visual test of xterm-256 foreground/background grids and gradient.
 * @property {TestFn} screen - Visual test of screen clearing/erasure controls.
 * @property {TestFn} cursor - Visual test of cursor movement/visibility.
 * @property {TestFn} reveal - Visual test of {@link ANSI#reveal}.
 * @property {TestFn} clean - Visual test of {@link ANSI#clean}.
 * @property {TestFn} all - Convenience runner for a representative subset.
 */

/**
 * 256-color helpers (xterm-256).
 * @typedef {Object} ANSIColor256
 * @property {function(number): string} fg  Foreground by palette index (0-255).
 * @property {function(number): string} bg  Background by palette index (0-255).
 */

/**
 * Truecolor (24-bit) helpers.
 * @typedef {Object} ANSIColorRGB
 * @property {function(number, number, number): string} fg  Foreground by RGB.
 * @property {function(number, number, number): string} bg  Background by RGB.
 */

/**
 * Centralized ANSI escape handling for styles, colors, cursor/screen control, and safe text formatting.
 *
 * Instances respect `process.env.NO_COLOR`: when defined, all formatting methods elide SGR sequences, producing plain text suitable for log files and non-ANSI consumers.
 *
 * @class ANSI
 * @classdesc Helper for ANSI SGR composition with safety guarantees (automatic reset) and utilities to stabilize multi-line indentation with active styles.
 *
 * @property {boolean} noColor - True when `NO_COLOR` is present in the environment. @default false (when `NO_COLOR` is unset)
 * @property {boolean} debug - Emit extra diagnostics in testing/cleaning utilities. @default false
 * @property {string} reset - Global SGR reset (`"\x1b[0m"`).
 * @property {ANSISimpleMap} style - Text style SGR codes (bold, underline, etc.).
 * @property {ANSIForegroundPalette} fg - Foreground palette with bright variants.
 * @property {ANSIBackgroundPalette} bg - Background palette with bright variants.
 * @property {ANSIPalette} hc - High-contrast palette (fg/bg).
 * @property {ANSIPalette} bc - Balanced-contrast palette (fg/bg).
 * @property {ANSICursor} cursor - Cursor movement/visibility helpers.
 * @property {ANSIScreen} screen - Screen clearing/erasure helpers.
 * @property {ANSIColor256} color256 - xterm-256 helpers.
 * @property {ANSIColorRGB} colorRGB - 24-bit truecolor helpers.
 * @property {ANSITestNamespace} test - Visual test functions bound to the instance.
 *
 * @example
 * const ansi = new ANSI();
 * console.log(ansi.format(ansi.fg.cyan, 'Cyan text!'));
 * @example <caption>Disable styling globally</caption>
 * process.env.NO_COLOR = '1';
 * const ansi = new ANSI();
 * console.log(ansi.format(ansi.fg.green, 'This will be plain text'));
 * @since 1.0.0
 */
class ANSI {
  /**
    * Create a new {@link ANSI} instance with precomputed palettes and helpers.
    *
    * Initializes:
    * – `noColor` (boolean) from `process.env.NO_COLOR`.
    * – `debug` (boolean) for verbose internal demos in `clean()` and tests.
    * – `style`, `fg`, `bg`, `hc`, `bc` (palettes), `cursor`, `screen`, `color256`, `colorRGB`.
    * – `test` namespace bound to the instance.
    *
    * @constructor
    * @example
    * const ansi = new ANSI();
    * if (ansi.noColor) console.log('Plain output mode');
    */
  constructor() {
    // -----------------------------------------------------------------------------
    /** Toggle behavior flags and base reset code. */

    // process.env.NO_COLOR = 1;
    /** Whether to disable color output based on NO_COLOR.
     * @type {boolean}
     * @default false — when the environment variable `NO_COLOR` is **unset**
     */
    this.noColor = process.env.NO_COLOR !== undefined; // Check if NO_COLOR is set
    this.debug = false;

    this.reset = "\x1b[0m";

    // -----------------------------------------------------------------------------
    /** Text style SGR (Select Graphic Rendition) codes — bold, underline, etc. */
    this.style = {
      bold: "\x1b[1m",
      dim: "\x1b[2m",
      italic: "\x1b[3m",
      underline: "\x1b[4m",
      blink: "\x1b[5m",
      reverse: "\x1b[7m",
      hidden: "\x1b[8m",
      strikethrough: "\x1b[9m",
      doubleUnderline: "\x1b[21m",
      framed: "\x1b[51m",
      encircled: "\x1b[52m",
      overlined: "\x1b[53m",
      resetBoldDim: "\x1b[22m",
      resetUnderline: "\x1b[24m",
      resetBlink: "\x1b[25m",
      resetReverse: "\x1b[27m",
      resetHidden: "\x1b[28m",
      resetStrikethrough: "\x1b[29m",
      resetOverlined: "\x1b[55m",
    };

    // -----------------------------------------------------------------------------
    /** Foreground color map, including bright variants. */
    this.fg = {
      reset: "\x1b[39m",
      black: "\x1b[30m",
      red: "\x1b[31m",
      green: "\x1b[32m",
      yellow: "\x1b[33m",
      blue: "\x1b[34m",
      magenta: "\x1b[35m",
      cyan: "\x1b[36m",
      white: "\x1b[37m",
      bright: {
        black: "\x1b[90m",
        red: "\x1b[91m",
        green: "\x1b[92m",
        yellow: "\x1b[93m",
        blue: "\x1b[94m",
        magenta: "\x1b[95m",
        cyan: "\x1b[96m",
        white: "\x1b[97m",
      },
    };

    // -----------------------------------------------------------------------------
    /** Background color map, including bright variants. */
    this.bg = {
      reset: "\x1b[49m",
      black: "\x1b[40m",
      red: "\x1b[41m",
      green: "\x1b[42m",
      yellow: "\x1b[43m",
      blue: "\x1b[44m",
      magenta: "\x1b[45m",
      cyan: "\x1b[46m",
      white: "\x1b[47m",
      bright: {
        black: "\x1b[100m",
        red: "\x1b[101m",
        green: "\x1b[102m",
        yellow: "\x1b[103m",
        blue: "\x1b[104m",
        magenta: "\x1b[105m",
        cyan: "\x1b[106m",
        white: "\x1b[107m",
      },
    };

    // -----------------------------------------------------------------------------
    /**
     * High-Contrast (hc) palette
     * Prefer the brightest legible foreground on solid backgrounds, to maximize
     * readability in tables/charts and dense CLI output.
     */
    this.hc = {
      // Foreground Colors
      fg: {
        black: this.fg.bright.black,   // Bright Black (Gray) text
        red: this.fg.bright.red,     // Bright Red text
        green: this.fg.bright.green,   // Bright Green text
        yellow: this.fg.bright.yellow,  // Bright Yellow text
        blue: this.fg.bright.blue,    // Bright Blue text
        magenta: this.fg.bright.magenta, // Bright Magenta text
        cyan: this.fg.bright.cyan,    // Bright Cyan text
        white: this.fg.bright.white,   // Bright White text
      },

      // Background Colors
      bg: {
        black: this.bg.black,      // Black background
        red: this.bg.red,        // Red background
        green: this.bg.green,      // Green background
        yellow: this.bg.yellow,     // Yellow background
        blue: this.bg.blue,       // Blue background
        magenta: this.bg.magenta,    // Magenta background
        cyan: this.bg.cyan,       // Cyan background
        white: this.bg.white,      // White background
      },
    };

    // -----------------------------------------------------------------------------
    /**
     * Balanced-Contrast (bc) palette
     * Trade a bit of contrast for aesthetics. Good for headings and accent text.
     */
    this.bc = {
      // Foreground Colors
      fg: {
        black: this.fg.bright.black,
        red: this.fg.bright.red,
        green: this.fg.bright.green,
        yellow: this.fg.yellow,
        blue: this.fg.bright.blue,
        magenta: this.fg.bright.magenta,
        cyan: this.fg.bright.cyan,
        white: this.fg.bright.white,
      },

      // Background Colors
      bg: {
        black: this.bg.black,
        red: this.bg.red,
        green: this.bg.green,
        yellow: this.bg.yellow,
        blue: this.bg.blue,
        magenta: this.bg.magenta,
        cyan: this.bg.cyan,
        white: this.bg.white,
      },
    };

    // -----------------------------------------------------------------------------
    /**
     * Cursor control helpers (movement/visibility).
     * NOTE: Tests also use cursor.save/restore; see File Header "Notes" on how to
     * add those if needed for your terminal.
     */
    this.cursor = {
      up: (n = 1) => `\x1b[${n}A`,
      down: (n = 1) => `\x1b[${n}B`,
      right: (n = 1) => `\x1b[${n}C`,
      left: (n = 1) => `\x1b[${n}D`,
      moveTo: (row, col) => `\x1b[${row};${col}H`,
      show: "\x1b[?25h",
      hide: "\x1b[?25l",
      // VT100 save/restore cursor (supported by many terminals)
      save: "\x1b[s",
      restore: "\x1b[u",
    };

    // -----------------------------------------------------------------------------
    /** Screen region helpers: clear page, regions, and line segments. */
    this.screen = {
      clear: "\x1b[2J",
      clearFromCursorToEnd: "\x1b[0J",
      clearFromCursorToStart: "\x1b[1J",
      clearLine: "\x1b[2K",
      clearLineToEnd: "\x1b[0K",
      clearLineToStart: "\x1b[1K",
    };

    // -----------------------------------------------------------------------------
    /** 256-color helpers (xterm palette). */
    this.color256 = {
      fg: (n) => `\x1b[38;5;${n}m`,
      bg: (n) => `\x1b[48;5;${n}m`,
    };

    // -----------------------------------------------------------------------------
    /** Truecolor (24-bit) helpers for precise RGB output. */
    this.colorRGB = {
      fg: (r, g, b) => `\x1b[38;2;${r};${g};${b}m`,
      bg: (r, g, b) => `\x1b[48;2;${r};${g};${b}m`,
    };

    // -----------------------------------------------------------------------------
    /**
     * Test namespace
     * Visual checks for styles, palettes, screen/cursor, and cleaner behavior.
     */
    this.test = {
      standard: this.testStandard.bind(this),
      color256: this.testColor256.bind(this),
      screen: this.testScreen.bind(this),
      cursor: this.testCursor.bind(this),
      reveal: this.testReveal.bind(this),
      clean: this.testClean.bind(this),
      all: this.testAll.bind(this), // Optional method to run all tests
    };
  }

  /**
    * Enable or disable verbose internal diagnostics for this instance.
    *
    * When enabled, visual helpers such as {@link ANSI#clean} will print annotated input/output using {@link ANSI#reveal} to help with troubleshooting.
    *
    * @param {boolean} value - `true` to enable debug diagnostics; `false` to disable.
    * @returns {void}
    * @since 1.0.0
    * @example
    * const ansi = new ANSI();
    * ansi.setDebug(true);
    * console.log(ansi.clean(ansi.fg.red + '  demo' + ansi.reset));
    */
  setDebug(value) {
    this.debug = value;
  }

  /**
    * Decide whether "white" or "black" text contrasts best against a color.
    *
    * Accepts either:
    * 1) An ANSI 256 index (`0–255`), or
    * 2) A **foreground** SGR escape like "\x1b[44m" is **not** supported (background);
    *    supported foreground codes are "\x1b[30–37m" and "\x1b[90–97m".
    *
    * Internals:
    * - Converts the index to RGB (0–15 set, 6×6×6 cube, grayscale).
    * - Computes relative luminance (WCAG-inspired) and compares to `threshold`.
    *
    * @param {string|number} input
    *   ANSI 256 index or single **foreground** SGR sequence.
    * @param {number} [threshold=0.2]
    *   Luminance cutoff; higher favors "black".
    * @returns {"white"|"black"}
    *   Recommended contrasting foreground color name.
    * @throws {Error}
    *   If `input` is neither a supported foreground SGR sequence nor a number.
    * @since 1.0.0
    */
  getContrastName(input, threshold = 0.2) {
    // Parse ANSI escape sequences (e.g., "\x1b[31m")
    function parseEscapeSequence(escapeSequence) {
      const match = /\x1b\[(\d+)m/.exec(escapeSequence);
      if (match) {
        const colorCode = parseInt(match[1], 10);
        if (colorCode >= 30 && colorCode <= 37) {
          // Map basic foreground colors
          return colorCode - 30;
        } else if (colorCode >= 90 && colorCode <= 97) {
          // Map bright foreground colors
          return colorCode - 90 + 8;
        }
      }
      throw new Error("Invalid ANSI escape sequence");
    }

    // Convert ANSI color code to RGB
    function ansiToRgb(ansi) {
      if (ansi < 16) {
        // Standard colors
        const baseColors = [
          [0, 0, 0], [128, 0, 0], [0, 128, 0], [128, 128, 0],
          [0, 0, 128], [128, 0, 128], [0, 128, 128], [192, 192, 192],
          [128, 128, 128], [255, 0, 0], [0, 255, 0], [255, 255, 0],
          [0, 0, 255], [255, 0, 255], [0, 255, 255], [255, 255, 255],
        ];
        return baseColors[ansi];
      } else if (ansi >= 16 && ansi < 232) {
        // 6x6x6 color cube
        const base = ansi - 16;
        return [
          Math.floor(base / 36) * 51,
          Math.floor((base % 36) / 6) * 51,
          (base % 6) * 51,
        ];
      } else if (ansi >= 232 && ansi < 256) {
        // Grayscale colors
        const gray = Math.floor((ansi - 232) * 10 + 8);
        return [gray, gray, gray];
      }
      throw new Error("Invalid ANSI color code");
    }

    // Calculate relative luminance of a color (perceived brightness)
    function getLuminance(rgb) {
      const [r, g, b] = rgb.map(channel => {
        const scaled = channel / 255;
        return scaled <= 0.03928 ? scaled / 12.92 : Math.pow((scaled + 0.055) / 1.055, 2.4);
      });
      return 0.2126 * r + 0.7152 * g + 0.0722 * b;
    }

    // Determine the ANSI code
    let ansiCode;
    if (typeof input === "string" && input.startsWith("\x1b[")) {
      ansiCode = parseEscapeSequence(input);
    } else if (typeof input === "number") {
      ansiCode = input;
    } else {
      throw new Error("Invalid input type");
    }

    const rgb = ansiToRgb(ansiCode);
    const luminance = getLuminance(rgb);

    // Return contrast color based on threshold
    return luminance > threshold ? "black" : "white";
  }

  /**
    * Return the **foreground** SGR code (`ansi.fg.white` or `ansi.fg.black`) that best contrasts with a given **background** index in the xterm-256 palette.
    *
    * @param {number} colorIndex - Background color index (`0..255`).
    * @returns {string} Foreground SGR sequence for optimal contrast.
    * @since 1.0.0
    * @example
    * process.stdout.write(ansi.getContrastFg(21) + ansi.color256.bg(21) + ' text ' + ansi.reset);
    */
  getContrastFg(colorIndex) {
    return this.getContrastName(colorIndex) === "white" ? this.fg.white : this.fg.black;
  }

  /**
    * Return the **background** SGR code (`ansi.bg.white` or `ansi.bg.black`) that best contrasts with a given **foreground** index in the xterm-256 palette.
    *
    * @param {number} colorIndex - Foreground color index (`0..255`).
    * @returns {string} Background SGR sequence for optimal contrast.
    * @since 1.0.0
    * @example
    * process.stdout.write(ansi.getContrastBg(196) + ansi.color256.fg(196) + ' text ' + ansi.reset);
    */
  getContrastBg(colorIndex) {
    return this.getContrastName(colorIndex) === "white" ? this.bg.white : this.bg.black;
  }

  /**
    * Annotate real SGR sequences inline so control bytes are visually inspectable.
    *
    * For each actual `\x1b[...m` in `text`, the function appends a printable `\\x1b[...m` representation immediately after it, allowing side-by-side verification of escape codes.
    *
    * @param {string} text - String that may contain SGR sequences.
    * @returns {string} Original string with printable annotations injected.
    * @since 1.0.0
    * @example
    * console.log(ansi.reveal(ansi.fg.red + 'Hi' + ansi.reset));
    */
  reveal(text) {
    const realAnsiRegex = /\x1b\[[0-9;]*m/g; // Matches real ANSI codes

    // Process real ANSI codes
    return text.replace(realAnsiRegex, (match) => {
      return `${match}${match.replace(/\x1b/g, '\\x1b')}`; // Add plain text representation alongside the real code
    });
  }

  /**
    * Update an ANSI active-code accumulator with the effect of a single SGR code.
    *
    * Behavior:
    * - For **foreground** (`30–37`, `90–97`) and **background** (`40–47`, `100–107`) SGR codes,
    *   this function **mutates** the provided `activeCodes` object in place.
    * - For common **formatting** codes (`1,2,3,4,5,7,8,9`), it **adds** entries to
    *   `activeCodes.formatting` (no deduping).
    * - For a **global reset** (`\x1b[0m`), the current implementation **reinitializes an
    *   internal accumulator reference** instead of mutating the fields on the object that
    *   was passed in. In practice this means the caller’s original `activeCodes` object
    *   will **not** be reset by this function when a global reset is encountered.
    *
    * Implication for callers:
    * - If you require a true in-place reset of the **same object instance**, you must
    *   reset its fields yourself before/after calling this function (e.g.,
    *   `activeCodes.foreground = ''; activeCodes.background = ''; activeCodes.formatting.length = 0;`).
    * - For non-reset SGR codes, you may safely rely on in-place mutation of `activeCodes`.
    *
    * @param {string} code
    *   A single SGR sequence (e.g., "\x1b[31m", "\x1b[0m").
    * @param {ANSIActiveCodes} activeCodes
    *   The accumulator object to be updated for non-reset codes.
    * @returns {void}
    * @since 1.0.0
    *
    * @example <caption>Foreground update (mutates object)</caption>
    * const acc = { foreground: '', background: '', formatting: [] };
    * categorizeCode('\x1b[31m', acc);
    * // acc.foreground is now set to the red SGR; same object instance is mutated.
    *
    * @example <caption>Global reset (does NOT reset the passed object)</caption>
    * const acc = { foreground: '\x1b[31m', background: '\x1b[44m', formatting: ['\x1b[1m'] };
    * categorizeCode('\x1b[0m', acc);
    * // acc is unchanged by this call; reset must be applied by the caller if desired.
    */
  categorizeCode(code, activeCodes) {
    if (code === this.reset) {
      // Reset all active codes (MUTATE IN PLACE — do NOT reassign the parameter)
      activeCodes.foreground = '';
      activeCodes.background = '';
      activeCodes.formatting.length = 0;
      return;
    }

    if (/^\x1b\[(3[0-7]|9[0-7])m$/.test(code)) {
      // Foreground colors (3x for normal, 9x for bright)
      activeCodes.foreground = code;
      return;
    }

    if (/^\x1b\[(4[0-7]|10[0-7])m$/.test(code)) {
      // Background colors (4x for normal, 10x for bright)
      activeCodes.background = code;
      return;
    }

    if (/^\x1b\[(1|2|3|4|5|7|8|9)m$/.test(code)) {
      // Text styles (bold, dim, italic, etc.)
      const style = code; // Keep the full style code
      if (!activeCodes.formatting.includes(style)) {
        activeCodes.formatting.push(style);
      }
    }
  }

  /**
    * Normalize ANSI text by preserving **leading indentation** while keeping styles.
    *
    * Many terminals “eat” indentation when a color code precedes spaces. `clean()` captures leading SGR + whitespace, then **replays** active styles *after* the spaces, so alignment remains stable across lines.
    *
    * Behavior:
    * – Preserves per-line indentation.
    * – Resets at the start of each processed line, then reapplies active fg/bg/styles.
    * – Ensures each line ends with full reset + fg/bg resets.
    * – Emits extra debug output when {@link ANSI#debug} is `true`.
    *
    * @param {string} inputString - Single or multi-line string with SGR codes.
    * @returns {string} A visually stable string with preserved indentation and safe resets.
    * @throws {never}
    * @since 1.0.0
    * @example
    * const s = `${ansi.fg.red}  ERROR:${ansi.reset} details`;
    * console.log(ansi.clean(s));
    */
  clean(inputString) {
    // return inputString;
    // Regex to match ANSI escape codes
    const ansiRegex = /\x1b\[[0-9;]*[a-zA-Z]/g;

    // Object to track active ANSI codes
    let activeCodes = {
      foreground: '',
      background: '',
      formatting: [],
    };

    // Split input string into lines
    const lines = inputString.split('\n');

    // Processed lines array
    const processedLines = [];

    // Iterate through each line
    for (const line of lines) {
      if (this.debug) {
        console.log(`\n+ line:\n${this.reveal(line)}${this.reset}`);
      }

      let leadingPart = '';
      let restOfLine = line;

      // Match ANSI codes and whitespace at the beginning of the line
      const leadingMatch = line.match(/^((?:\x1b\[[0-9;]*[a-zA-Z])*\s*)/);

      if (leadingMatch) {
        leadingPart = leadingMatch[0];
        restOfLine = line.slice(leadingPart.length).trim();
      }

      // Extract ANSI codes and spaces from the leading part
      const leadingAnsiCodes = leadingPart.match(ansiRegex) || [];
      const leadingSpaces = leadingPart.replace(ansiRegex, '');

      // Process ANSI codes in the leading part to update active codes
      for (const code of leadingAnsiCodes) {
        this.categorizeCode(code, activeCodes);
      }

      // Reconstruct leading whitespace
      const activeCodesString = [
        activeCodes.foreground,
        activeCodes.background,
        ...activeCodes.formatting
      ].filter(Boolean).join('');
      const reconstructedLeadingWhitespace = `${this.reset}${leadingSpaces}${activeCodesString}`;

      // Process the remaining part of the line for additional ANSI codes
      const restAnsiCodes = restOfLine.match(ansiRegex) || [];
      for (const code of restAnsiCodes) {
        this.categorizeCode(code, activeCodes);
      }

      // Reconstruct the processed line
      const processedLine = `${reconstructedLeadingWhitespace}${restOfLine}${this.reset}${this.fg.reset}${this.bg.reset}`;

      if (this.debug) {
        console.log(`+ processedLine:\n${this.reveal(processedLine)}${this.reset}`);
      }

      processedLines.push(processedLine);
    }

    // Recombine processed lines into a single string
    return processedLines.join('\n');
  }

  /**
    * Format one or more messages with an opening SGR `code` and a guaranteed closing reset.
    *
    * Honors `NO_COLOR` by removing both open/close codes when styling is disabled. Also replaces `[[ANSI_ON]]` and `[[ANSI_OFF]]` placeholders inside the message stream with the actual open/reset for this invocation.
    *
    * @param {string} code - Opening SGR sequence (concatenate as needed, e.g., `ansi.fg.green + ansi.style.bold`). Falsy implies just a reset wrapper.
    * @param {...*} messages - Values that will be stringified and joined with a single space.
    * @returns {string} Cleaned string with safe resets. `[[ANSI_ON]]` and `[[ANSI_OFF]]` are expanded to this call’s open/reset (or empty when `NO_COLOR` is set).
    * @throws {never}
    * @since 1.0.0
    * @example
    * ansi.format(ansi.fg.green + ansi.style.bold, 'OK', 200); // bold green
    * @example
    * ansi.format('', '[[ANSI_ON]]X[[ANSI_OFF]]'); // expands placeholders with open/reset
    */
  format(code, ...messages) {
    const ansiCode = this.noColor ? '' : (code || this.reset); // Disable ANSI if NO_COLOR is set
    const resetCode = this.noColor ? '' : this.reset; // Disable reset if NO_COLOR is set
    const text = messages.join(" "); // Concatenate all arguments into a single string
    return this.clean(`${ansiCode}${text}${resetCode}`
      .replace(/\[\[ANSI_ON\]\]/g, ansiCode)
      .replace(/\[\[ANSI_OFF\]\]/g, resetCode));
  }

  /**
    * Remove `[[ANSI_ON]]` / `[[ANSI_OFF]]` placeholders, leaving any real SGR codes intact.
    *
    * Typical use is when you previously inserted placeholders (via {@link format})
    * and now need a plain-text version without the placeholder markers.
    *
    * @param {string} input
    *   Raw string that may include `[[ANSI_ON]]` and `[[ANSI_OFF]]` placeholders.
    * @returns {string}
    *   Plain text with placeholders removed. Existing SGR escape sequences are not stripped.
    * @since 1.0.0
    *
    * @example
    * const raw = ansi.format(ansi.fg.yellow, '[[ANSI_ON]]warn[[ANSI_OFF]]');
    * ansi.stripTags(raw); // → "warn" (any existing SGR escapes remain)
    */
  stripTags(input) {
    if (typeof input !== 'string') return input;
    return input
      .replace(/\[\[ANSI_ON\]\]/g, '')   // remove pseudo on
      .replace(/\[\[ANSI_OFF\]\]/g, '');  // remove pseudo off
  }

  /**
    * Visual test for basic styles and standard/bright color sets.
    *
    * Writes a showcase to `stdout` and leaves the terminal in a reset state. Intended for manual verification; output may vary by terminal emulator.
    *
    * @returns {void}
    * @since 1.0.0
    */
  testStandard() {
    console.log(`${this.reset}=== Testing ANSI Codes ===`);

    // Test Reset
    console.log(`${this.reset}[Reset] This text should be in default style.`);

    // Test Styles
    console.log(`${this.style.bold}[Bold] This text is bold.${this.reset}`);
    console.log(`${this.style.dim}[Dim] This text is dimmed.${this.reset}`);
    console.log(`${this.style.italic}[Italic] This text is italicized.${this.reset}`);
    console.log(`${this.style.underline}[Underline] This text is underlined.${this.reset}`);
    console.log(`${this.style.blink}[Blink] This text blinks (may not be supported).${this.reset}`);
    console.log(`${this.style.reverse}[Reverse] This text has reversed colors.${this.reset}`);
    console.log('[Hidden] Following line is hidden style:');
    console.log(`${this.style.hidden}[Hidden] This text is hidden.${this.reset}`);
    console.log(`${this.style.strikethrough}[Strikethrough] This text is struck through.${this.reset}`);
    console.log(`${this.style.doubleUnderline}[Double Underline] This text is double underlined.${this.reset}`);
    console.log(`${this.style.framed}[Framed] This text is framed (if supported).${this.reset}`);
    console.log(`${this.style.encircled}[Encircled] This text is encircled (if supported).${this.reset}`);
    console.log(`${this.style.overlined}[Overlined] This text is overlined.${this.reset}`);

    // Test Foreground Colors
    console.log("\n=== Foreground Colors ===");
    for (const [color, code] of Object.entries(this.fg)) {
      if (typeof code === 'object') continue; // Skip nested objects (e.g., "bright")
      console.log(`${code}[${color}] This is ${color} text.${this.fg.reset}`);
    }

    // Test Bright Foreground Colors
    console.log("\n=== Bright Foreground Colors ===");
    for (const [color, code] of Object.entries(this.fg.bright)) {
      console.log(`${code}[Bright ${color}] This is bright ${color} text.${this.fg.reset}`);
    }

    // Test Background Colors
    console.log("\n=== Background Colors ===");
    for (const [color, code] of Object.entries(this.bg)) {
      if (typeof code === 'object') continue; // Skip nested objects (e.g., "bright")
      console.log(`${code}[${color}] This is text with a ${color} background.${this.bg.reset}`);
    }

    // Test Bright Background Colors
    console.log("\n=== Bright Background Colors ===");
    for (const [color, code] of Object.entries(this.bg.bright)) {
      console.log(`${code}[Bright ${color}] This is text with a bright ${color} background.${this.bg.reset}`);
    }
  }

  /**
    * Visual test for the xterm-256 palette.
    *
    * Shows:
    * – Foreground grid (auto-contrast background).
    * – Background grid (auto-contrast foreground).
    * – A curated gradient inspired by Franz Schröder’s tool.
    *
    * @returns {void}
    * @since 1.0.0
    * @example
    * // Quick contrast probe
    * for (let i = 16; i < 24; i++) {
    * process.stdout.write(ansi.getContrastFg(i) + ansi.color256.bg(i) + `${i}`);
    * }
    * process.stdout.write(ansi.reset + '\n');
    */
  testColor256() {
    // Test 256-Color Mode
    console.log("\n=== 256-Color Foreground ===");

    const rowLenStandard = 8;
    const rowLen216StepRGB = 6;
    const rowLenGrayscale = 12;
    const rowCountStandard = 2;
    const rowCount216StepRGB = 6 * 6;
    // const rowCountGrayscale = 2;
    const indexCountStandard = rowLenStandard * rowCountStandard;
    const indexCount216StepRGB = rowLen216StepRGB * rowCount216StepRGB;
    // const indexCountGrayscale = rowLenGrayscale * rowCountGrayscale;
    // const indexStartStandard = 0;
    const indexStart216StepRGB = indexCountStandard;
    const indexStartGrayscale = indexCountStandard + indexCount216StepRGB;

    const getRowLen = (index) => {
      if (index < indexStart216StepRGB) {
        return rowLenStandard; // Standard colors
      } else if (index < indexStartGrayscale) {
        return rowLen216StepRGB; // 216 RGB colors
      } else {
        return rowLenGrayscale; // Grayscale
      }
    };

    // Display 256-color foreground in 2D
    for (let i = 0; i < 256; i += getRowLen(i)) {
      let row = "";
      for (let j = 0; j < getRowLen(i); j++) {
        const colorIndex = i + j;
        if (colorIndex >= 256) break; // Ensure we don't exceed the 256-color range
        row += `${this.getContrastBg(colorIndex)}${this.color256.fg(colorIndex)} ${colorIndex.toString().padStart(3, " ")} `;
      }
      console.log(row + this.reset);
    }

    console.log("\n=== 256-Color Background ===");

    // Display 256-color background in 2D
    for (let i = 0; i < 256; i += getRowLen(i)) {
      let row = "";
      for (let j = 0; j < getRowLen(i); j++) {
        const colorIndex = i + j;
        row += `${this.getContrastFg(colorIndex)}${this.color256.bg(colorIndex)} ${colorIndex.toString().padStart(3, " ")} `;
      }
      console.log(row + this.reset);
    }

    // console.log("\n=== 256-Color Background Pixel Style ===");

    // // Display 256-color background in 2D
    // for (let i = 0; i < 256; i += getRowLen(i)) {
    //   let row = "";
    //   for (let j = 0; j < getRowLen(i); j++) {
    //     const colorIndex = i + j;
    //     row += `${this.color256.bg(colorIndex)}  `;
    //   }
    //   console.log(row + this.reset);
    // }

    console.log("\n=== 256-Color Gradient ===");
    console.log(`\nCredit: F. Schröder (${this.fg.cyan + this.style.underline}https://www.hackitu.de/termcolor256/${this.reset})`);

    // Define a manual 2D grid of ANSI color indexes
    const colorGrids = [
      {
        name: 'Layer 1 (Outermost)',
        grid: [
          [16, 52,  88, 124, 160, 196, 203, 210, 217, 224, 231],
          [16, 52,  88, 124, 160, 202, 209, 216, 223, 230, 231],
          [16, 52,  88, 124, 166, 208, 215, 222, 229, 230, 231],
          [16, 52,  88, 130, 172, 214, 221, 228, 229, 230, 231],
          [16, 52,  94, 136, 178, 220, 227, 228, 229, 230, 231],
          [16, 58, 100, 142, 184, 226, 227, 228, 229, 230, 231],
          [16, 22,  64, 106, 148, 190, 227, 228, 229, 230, 231],
          [16, 22,  28,  70, 112, 154, 191, 228, 229, 230, 231],
          [16, 22,  28,  34,  76, 118, 155, 192, 229, 230, 231],
          [16, 22,  28,  34,  40,  82, 119, 156, 193, 230, 231],
          [16, 22,  28,  34,  40,  46,  83, 120, 157, 194, 231],
          [16, 22,  28,  34,  40,  47,  84, 121, 158, 195, 231],
          [16, 22,  28,  34,  41,  48,  85, 122, 159, 195, 231],
          [16, 22,  28,  35,  42,  49,  86, 123, 159, 195, 231],
          [16, 22,  29,  36,  43,  50,  87, 123, 159, 195, 231],
          [16, 23,  30,  37,  44,  51,  87, 123, 159, 195, 231],
          [16, 17,  24,  31,  38,  45,  87, 123, 159, 195, 231],
          [16, 17,  18,  25,  32,  39,  81, 123, 159, 195, 231],
          [16, 17,  18,  19,  26,  33,  75, 117, 159, 195, 231],
          [16, 17,  18,  19,  20,  27,  69, 111, 153, 195, 231],
          [16, 17,  18,  19,  20,  21,  63, 105, 147, 189, 231],
          [16, 17,  18,  19,  20,  57,  99, 141, 183, 225, 231],
          [16, 17,  18,  19,  56,  93, 135, 177, 219, 225, 231],
          [16, 17,  18,  55,  92, 129, 171, 213, 219, 225, 231],
          [16, 17,  54,  91, 128, 165, 207, 213, 219, 225, 231],
          [16, 53,  90, 127, 164, 201, 207, 213, 219, 225, 231],
          [16, 52,  89, 126, 163, 200, 207, 213, 219, 225, 231],
          [16, 52,  88, 125, 162, 199, 206, 213, 219, 225, 231],
          [16, 52,  88, 124, 161, 198, 205, 212, 219, 225, 231],
          [16, 52,  88, 124, 160, 197, 204, 211, 218, 225, 231]
        ]
      },
      {
        name: 'Layer 2',
        grid: [
          [59,  95, 131, 167, 174, 181, 188],
          [59,  95, 131, 173, 180, 187, 188],
          [59,  95, 137, 179, 186, 187, 188],
          [59, 101, 143, 185, 186, 187, 188],
          [59,  65, 107, 149, 186, 187, 188],
          [59,  65,  71, 113, 150, 187, 188],
          [59,  65,  71,  77, 114, 151, 188],
          [59,  65,  71,  78, 115, 152, 188],
          [59,  65,  72,  79, 116, 152, 188],
          [59,  66,  73,  80, 116, 152, 188],
          [59,  60,  67,  74, 116, 152, 188],
          [59,  60,  61,  68, 110, 152, 188],
          [59,  60,  61,  62, 104, 146, 188],
          [59,  60,  61,  98, 140, 182, 188],
          [59,  60,  97, 134, 176, 182, 188],
          [59,  96, 133, 170, 176, 182, 188],
          [59,  95, 132, 169, 176, 182, 188],
          [59,  95, 131, 168, 175, 182, 188]
        ]
      },
      {
        name: 'Layer 3 (Innermost)',
        grid: [
          [102, 138, 144, 108, 109, 103, 139, 145]
        ]
      },
    ];

    // Iterate through the grids
    colorGrids.forEach(({ name, grid }, gridIndex) => {
      // Display the grid name or assign a default name
      // const gridName = name || `Grid ${gridIndex + 1}`;
      // console.log(`\n${this.format(this.style.bold, gridName)}:`);
      if (name) {
        console.log(`\n${this.format(this.style.bold, name)}:`);
      } else {
        console.log('');
      }

      // Iterate through the rows in the grid
      if (Array.isArray(grid) && grid.length > 0) {
        grid.forEach((row, rowIndex) => {
          if (Array.isArray(row)) {
            let rowDisplay = "";
            row.forEach((colorIndex) => {
              if (Number.isInteger(colorIndex) && colorIndex >= 0 && colorIndex <= 255) {
                rowDisplay += `${this.getContrastFg(colorIndex)}${this.color256.bg(colorIndex)} ${colorIndex.toString().padStart(3, ' ')} `;
              } else {
                rowDisplay += `${this.format(this.bc.red, "ERR")} ??? `;
              }
            });

            var line = rowDisplay + this.reset;

            console.log(line);
          } else {
            console.log(`Row \`${rowIndex + 1}\` is not a valid array`);
          }
        });
      } else {
        console.log(`Grid \"${name || ''}\" is empty or invalid.`);
      }
    });
  }

  /**
    * Visual test for screen clearing and region erasure controls.
    *
    * Demonstrates:
    * – `clear`, `clearFromCursorToEnd`, `clearFromCursorToStart`
    * – `clearLine`, `clearLineToEnd`, `clearLineToStart`
    *
    * Uses timed pauses to keep each step visible in most terminals.
    *
    * @returns {void}
    * @since 1.0.0
    */
  testScreen() {
    const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

    async function screenDemo() {
      console.log(`${this.reset}=== Testing Screen Controls ===`);

      // Clear the screen
      console.log("\nClearing the entire screen...");
      console.log("Waiting for 3 seconds before clearing the screen...");
      await sleep(3000);
      console.log(this.screen.clear);

      // Clear from cursor to end
      console.log("\nFilling the screen, then clearing from cursor to end...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      for (let i = 0; i < 10; i++) {
        console.log(`Line ${i + 1}: Filled with text`);
      }
      process.stdout.write(this.cursor.moveTo(5, 5)); // Move cursor to row 5, column 5
      console.log(`${this.screen.clearFromCursorToEnd}Cleared from cursor to the end of the screen.`);

      // Clear from cursor to start
      console.log("\nClearing from cursor to start...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(7, 20)); // Move cursor to row 7, column 20
      console.log(`${this.screen.clearFromCursorToStart}Cleared from cursor to the start of the screen.`);

      // Clear line
      console.log("\nClearing an entire line...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(9, 0)); // Move cursor to row 9
      console.log(`This line will be cleared shortly!${this.screen.clearLine}`);

      // Clear line from cursor to end
      console.log("\nClearing from cursor to the end of the line...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(10, 10)); // Move cursor to row 10, column 10
      console.log(`Partially cleared line${this.screen.clearLineToEnd}`);

      // Clear line from cursor to start
      console.log("\nClearing from cursor to the start of the line...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(11, 30)); // Move cursor to row 11, column 30
      console.log(`${this.screen.clearLineToStart}Remaining part of the line cleared.`);

      // Cursor and screen movement demo
      console.log("\nPerforming a cursor and screen movement demo...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      console.log(this.screen.clear); // Clear the screen
      for (let i = 1; i <= 5; i++) {
        process.stdout.write(this.cursor.moveTo(i, i * 5));
        console.log(`Row ${i}, Column ${i * 5}`);
        await sleep(500);
      }

      // Restore the cursor position
      console.log("\nTesting save and restore cursor position...");
      console.log("Waiting for 3 seconds before action...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(15, 10));
      console.log("Saved cursor position here!");
      process.stdout.write(this.cursor.save);
      await sleep(3000);

      process.stdout.write(this.cursor.moveTo(20, 20));
      console.log("Moved cursor to a different position!");
      await sleep(3000);

      process.stdout.write(this.cursor.restore);
      console.log("Restored to the saved cursor position.");

      console.log("\n=== End of Screen Controls Demo ===");
    }

    screenDemo();
  }

  /**
    * Visual test for cursor movement and visibility toggles.
    *
    * Exercises:
    * – `up`, `down`, `left`, `right`, `moveTo`
    * – `hide` / `show`
    * – `save` / `restore` (VT100 compatible; support varies by terminal)
    *
    * @returns {void}
    * @since 1.0.0
    */
  testCursor() {
    const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

    async function cursorDemo() {
      console.log(`${this.reset}=== Testing Cursor Controls ===`);

      // Move cursor up
      console.log("\nMoving the cursor up...");
      console.log("Waiting for 3 seconds before moving the cursor...");
      await sleep(3000);
      process.stdout.write("This is the original position.\n".repeat(5)); // Add some lines
      process.stdout.write(this.cursor.up(3)); // Move cursor up 3 lines
      console.log("Cursor moved up 3 lines!");

      // Move cursor down
      console.log("\nMoving the cursor down...");
      console.log("Waiting for 3 seconds before moving the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.down(2)); // Move cursor down 2 lines
      console.log("Cursor moved down 2 lines!");

      // Move cursor right
      console.log("\nMoving the cursor to the right...");
      console.log("Waiting for 3 seconds before moving the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.right(10)); // Move cursor 10 columns to the right
      console.log("Cursor moved 10 columns to the right!");

      // Move cursor left
      console.log("\nMoving the cursor to the left...");
      console.log("Waiting for 3 seconds before moving the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.left(5)); // Move cursor 5 columns to the left
      console.log("Cursor moved 5 columns to the left!");

      // Move cursor to specific position
      console.log("\nMoving the cursor to a specific position (row 10, column 20)...");
      console.log("Waiting for 3 seconds before moving the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(10, 20)); // Move cursor to row 10, column 20
      console.log("Cursor moved to row 10, column 20!");

      // Save and restore cursor position
      console.log("\nTesting save and restore cursor position...");
      console.log("Waiting for 3 seconds before saving the cursor position...");
      await sleep(3000);
      process.stdout.write(this.cursor.save); // Save the current cursor position
      console.log("Cursor position saved!");
      console.log("Waiting for 3 seconds before moving the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.moveTo(15, 5)); // Move cursor to a different position
      console.log("Cursor moved to row 15, column 5!");
      console.log("Waiting for 3 seconds before restoring the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.restore); // Restore the saved cursor position
      console.log("Cursor restored to the saved position!");

      // Hide and show cursor
      console.log("\nHiding the cursor...");
      console.log("Waiting for 3 seconds before hiding the cursor...");
      await sleep(3000);
      process.stdout.write(this.cursor.hide); // Hide the cursor
      console.log("Cursor is now hidden!");
      console.log("Waiting for 3 seconds before showing the cursor again...");
      await sleep(3000);
      process.stdout.write(this.cursor.show); // Show the cursor
      console.log("Cursor is now visible again!");

      console.log("\n=== End of Cursor Controls Demo ===");
    }

    cursorDemo();
  }

  /**
    * Visual test illustrating how {@link ANSI#reveal} annotates real SGR codes inline.
    *
    * @returns {void}
    * @since 1.0.0
    */
  testReveal() {
    const testString = `${this.fg.red}${this.style.underline}This is red underlined text\n    ${this.fg.green}${this.style.underline}This is green underlined text\n        This is green underlined text again plus ${this.fg.red}This is red underlined text${this.reset}        \nNo ANSI codes here`;
    console.log(`++ testString:\n${testString}`);
    console.log(`++ ANSI.reveal(testString):\n${this.reveal(testString)}`);
  }

  /**
    * Visual test comparing raw vs. cleaned multi-line ANSI output, including indentation preservation and optional debug reveals.
    *
    * @returns {void}
    * @since 1.0.0
    */
  testClean() {
    const testString = `${this.fg.red}${this.style.underline}This is red underlined text\n    ${this.fg.green}${this.style.underline}This is green underlined text\n        This is green underlined text again plus ${this.fg.red}This is red underlined text${this.reset}        \nNo ANSI codes here`;
    console.log(`++ testString:\n${testString}`);
    console.log(`++ [debug] ANSI.reveal(testString):\n${this.reveal(testString)}`);
    console.log(`++ ANSI.clean(testString):\n${this.clean(testString)}`);
  }

  /**
    * Run a representative subset of all visual tests in sequence.
    *
    * This is a convenience runner for quick manual verification.
    *
    * @returns {void}
    * @since 1.0.0
    */
  testAll() {
    this.test.standard();
    this.test.color256();
    this.test.screen();
    this.test.cursor();
    this.test.reveal();
    this.test.clean();
  }
}

/**
 * Public API (CommonJS export surface).
 *
 * Exported members:
 * - {@link ANSI} — The primary class providing rich ANSI/SGR styling utilities,
 *   cursor/screen controls, 256-color & RGB helpers, safe `format()` and `clean()`,
 *   and a small `test` namespace for manual verification.
 *
 * Versioning and stability:
 * - This export surface is considered public and stable as of {@link module:cure-ansi|cure-ansi} v1.0.0.
 * - New methods may be added to {@link ANSI} in minor versions; removals or
 *   breaking changes will only occur in major versions.
 *
 * Usage:
 * - When installed as a package, require by name.
 * - When used locally within this repository, require the file path.
 *
 * @since 1.0.0
 *
 * @example <caption>Package usage</caption>
 * const { ANSI } = require('cure-ansi');
 * const ansi = new ANSI();
 * console.log(ansi.format(ansi.fg.green + ansi.style.bold, 'Hello world'));
 *
 * @example <caption>Local usage (in this repo)</caption>
 * const { ANSI } = require('./index');
 * const ansi = new ANSI();
 * console.log(ansi.clean(ansi.fg.cyan + '  aligned text' + ansi.reset));
 *
 * @example <caption>Minimal logger-style usage</caption>
 * const { ANSI } = require('cure-ansi');
 * const ansi = new ANSI();
 * const ok = ansi.format(ansi.fg.green, 'OK');
 * const warn = ansi.format(ansi.fg.yellow, 'WARN');
 * const err = ansi.format(ansi.fg.red, 'ERROR');
 * console.log(ok, warn, err, ansi.reset);
 */
module.exports = { ANSI };
